# stock-news

Npx supabase init npx supabase functions new stock_sync_news npx supabase login
npx supabase link --project-ref your-project-id npx supabase secrets set
GEMINI_API_KEY= npx supabase functions deploy func_name --use-api

npx supabase functions deploy stock_sync_news --project-ref

curl https://generativelanguage.googleapis.com/v1beta/models?key=

npx supabase start

deno run --allow-net --allow-env --allow-read
--env-fil/supabase/functions/stock_sync_news/.env" "c:/New
Stuff/sns/stock_sync_news/index.ts"

curl -X POST http://127.0.0.1:8000 -H "Content-Type: application/json" -d
"{\"ticker\":\"AAPL\"}"
